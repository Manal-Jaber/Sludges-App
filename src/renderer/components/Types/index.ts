export interface Point {
  point: string;
  x: number;
  y: number;
  r: number;
  z: number;
  color?: string;
}
export interface data {
  id: number[];
  name: string[];
  x: number[];
  y: number[];
  z: number[][];
}
